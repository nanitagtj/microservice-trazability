server:
  port: 8003

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: trazability
  jpa:
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: "*"